import 'package:flutter/material.dart';

class ProductDetailScreen extends StatelessWidget {
  static const routeName = '/products-deail';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
